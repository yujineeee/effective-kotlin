package com.study.chapter01.item05.a_intro

fun pop(num: Int = 1) {
    /*
    * 어우 도저히 동작하게는 못짜겠다...
    require(num <= size) {
        "cannot remove more elements than current size"
    }
    check(isOpen) {
        "cannot pop from closed stack"
    }
    val ret = collection.take(num)
    collection = collection.drop(num)
    assert(ret.size == num)
    return ret
    */
}

